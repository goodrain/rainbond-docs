---
title: 安装前了解 Rainbond 概念
description: 第一次接触 Rainbond 时，先用一页了解团队、应用、组件、网关、应用模版和应用市场这些核心概念。
keywords:
- rainbond 核心概念
- rainbond 团队 应用 组件
- rainbond 应用市场
---

如果你是第一次接触 Rainbond，先记住这条主线就够了：你通常会先进入一个 `团队`，在团队里创建 `应用`，再把应用拆成多个 `组件` 运行；需要对外访问时交给 `网关`，需要复用和交付时就把应用沉淀为 `RAM`，再通过 `应用市场` 安装和分发。

<img src="/img/svg/quick-start-core-concepts.svg" alt="Rainbond 核心概念速览图" width="100%" />

## 先记住这 6 个概念

- **团队**：多人协作和资源隔离的工作空间，成员、权限、应用通常都在团队内管理。
- **应用**：一个完整业务系统的集合，例如商城、门户、支付平台。
- **组件**：应用里的运行单元，例如前端、后端 API、MySQL、Redis。
- **网关**：把外部请求转发到应用或组件的入口，通常对应域名、证书和访问策略。
- **应用模版**：可以理解为把一个应用及其依赖关系打包成可安装、可升级、可复用的模板。
- **应用市场**：安装和分发应用模版的地方，既能复用官方现成应用，也能复用企业内部沉淀下来的模板。

## 下一步

- 回到 [快速安装](../quick-start/quick-install)，启动你的体验环境。
- 安装完成后继续 [部署你的第一个应用](../quick-start/getting-started)。
